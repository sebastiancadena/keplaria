[project]
name = "keplaria-ingress"
version = "0.1.0"
description = "Pub/Sub push adapter fronting the Agent Runtime graph"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "google-auth>=2.35",
    "google-cloud-firestore>=2.19,<3",
    "httpx>=0.27",
    "pydantic>=2.9",
    "uvicorn>=0.32",
]
