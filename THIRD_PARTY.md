# Third-party code and asset provenance

Auditable ledger of pre-existing and third-party material in or behind this
project, per the hackathon's disclosure requirements. All repository commits
fall inside the submission period; the entries below are the components that
did not originate here.

**Repository license:** this repository is not currently published under an
open-source license (all rights reserved by the author). The third-party
components below remain under their own licenses, including the Apache-2.0
notices retained on files derived from the Google scaffold.

## Code

| Component | Origin | License | How it is used |
|---|---|---|---|
| Project scaffold | Generated by `google-agents-cli` 1.3.1 (`agents-cli scaffold`), Google LLC | Apache-2.0 | Initial skeleton (FastAPI app glue in `app/app_utils/` and `app/fast_api_app.py`, `deployment/terraform/`, test and eval scaffolding). Heavily adapted since; original copyright headers retained on derived files |
| `app/app_utils/reasoning_engine_adapter.py` | Ported from a `google-agents-cli` `agent_runtime` scaffold, Google LLC | Apache-2.0 | Serves the `reasoning_engine` HTTP contract on Agent Runtime; adapted for this app |
| `.agents/skills/google-agents-cli-*` | Reference skill documents shipped with the `agents-cli` scaffold, Google LLC | Apache-2.0 | Pinned agent-facing documentation; not application code |
| Python dependencies | PyPI, as declared in `pyproject.toml` / locked in `uv.lock` (google-adk, a2a-sdk, httpx, google-cloud-firestore, …) | Each package's own OSS license | Installed dependencies; none vendored into the repo |

## Deployed services (not vendored)

| Component | Origin | License | How it is used |
|---|---|---|---|
| yente | OpenSanctions (opensanctions/yente) | MIT | Screening API, self-hosted via Docker on a private VM (`infra/yente/`) |
| Elasticsearch | Elastic N.V. | Elastic License 2.0 / SSPL (free tier) | yente's index backend, same Docker stack |
| ERPNext on Frappe Cloud | Frappe Technologies | GPL-3.0 (used as SaaS) | The ERP system of record the executor writes to; no ERPNext code in this repo |

## Data

| Component | Origin | Notes |
|---|---|---|
| `fixtures/watchlist/entities.ftm.json` | Authored for this project | Synthetic entities in the FollowTheMoney format; contains **no** OpenSanctions or other third-party dataset content (see `fixtures/watchlist/README.md`) |
| Demo/case documents | Authored for this project | Synthetic; personal-like fields are fictional |

## Assets

No third-party fonts, icons, images, or audio are vendored in this repository.
Update this section before submission if the demo site or video introduces any.

## Tooling disclosure

Code, tests, and documentation were developed with AI coding assistance
(Anthropic Claude Code), reviewed and directed by the author.
